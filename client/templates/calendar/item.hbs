<div class="wrapper {{className}}" data-id="{{cid}}">
    <p><strong>{{appointment}}</strong></p>
    <p>{{trainerFirstName}} {{trainerLastName}}</p>
    <p>{{facilityName}}</p>
    {{#if scheduled }}
        <p>Your reservation</p>
    {{else}}
        <div class="add-appointment"><div class="add">+</div></div>
    {{/if}}
</div>